# Optional: a throwaway PostgreSQL for local development.
# Point DATABASE_URL at postgres://sacc:sacc@localhost:5433/sacc to use it.
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sacc
      POSTGRES_USER: sacc
      POSTGRES_PASSWORD: sacc
    ports:
      - "5433:5432"
    volumes:
      - sacc_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sacc -d sacc"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  sacc_pgdata:
